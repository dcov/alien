part of 'routing_test.dart';

/// Mock [RootRouting] for testing.
class MockRootRouting extends Mock implements RootRouting { }

/// Mock [Target] for testing.
class MockTarget extends Mock implements Target { }

/// Mock [Routing] for testing.
class MockRouting extends Mock implements Routing { }

